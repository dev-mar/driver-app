import 'package:flutter/material.dart';

/// Paleta centralizada para la app de conductor.
class AppColors {
  AppColors._();

  static const Color primary = Color(0xFFFFD600); // Amarillo Texi
  static const Color secondary = Color(0xFFFFFFFF);
  static const Color background = Color(0xFF000000);
  static const Color surface = Color(0xFF333026);
  /// Tarjetas y bloques de formulario (mayor contraste vs. fondo e inputs).
  static const Color surfaceCard = Color(0xFF121212);
  /// Relleno de campos de texto en formularios oscuros.
  static const Color inputFill = Color(0xFF1E1E1E);

  static const Color error = Color(0xFFE53935);
  static const Color success = Color(0xFF43A047);
  static const Color textPrimary = Color(0xFFFFFFFF);
  static const Color textSecondary = Color(0xFFB0B0B0);
  static const Color border = Color(0xFF454545);
  static const Color onPrimary = Color(0xFF000000);
}

